services:
  postgres:
    image: postgres:16-alpine
    container_name: graphql_blog_db
    environment:
      POSTGRES_DB: graphql_blog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
